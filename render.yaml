services:
  - type: web
    name: telegram-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: DEVELOPER_ID
        sync: false
      - key: WEBHOOK_URL
        sync: false
      - key: PORT
        value: "10000"
